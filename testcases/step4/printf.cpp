#include <iostream>
#include <stdlib.h>

int main(int argc, char ** argv){
  //std::string inputFile;
  for (int i = 0 ; i < 3000;i++)
    std::cout<<"\nYes, printfDir program in directory /testcase/step4 works!\n";
  /*
  for (int i = 1; i < argc; i++){
    std::cout << "(" << argv[i] << ") ";
  }
  std::cout<<"\nThis is the end of args.\n";
  
  while (std::getline (std::cin, inputFile)){
    std::cout <<inputFile<<std::endl;
  }
  std::cout<<"This is the end of std::cin.\n";
  if (!std::cin){
    std::cerr << "std::cin fails!" << std::endl;
  }
  */
  return EXIT_SUCCESS;
}
